<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- When building with .NET 8 SDK, only build net8.0-ios -->
    <TargetFrameworks Condition="'$(NETCoreSdkVersion)' &lt; '9.0'">net8.0-ios</TargetFrameworks>
    <!-- When building with .NET 9+ SDK, build net9.0-ios and net10.0-ios -->
    <TargetFrameworks Condition="'$(NETCoreSdkVersion)' &gt;= '9.0'">net9.0-ios;net10.0-ios</TargetFrameworks>
    <SupportedOSPlatformVersion>12.0</SupportedOSPlatformVersion>
    <NoWarn>$(NoWarn);NETSDK1202;NU5123;CS8767</NoWarn>
    <Nullable>enable</Nullable>
    <ImplicitUsings>true</ImplicitUsings>
    <IsBindingProject>true</IsBindingProject>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <AssemblyName>Datadog.MAUI.iOS.Internal</AssemblyName>

    <!-- NuGet package properties-->
    <PackageId>Datadog.MAUI.iOS.Internal</PackageId>
    <Authors>Datadog</Authors>
    <Description>.NET Bindings for Datadog iOS SDK Internal framework</Description>
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://github.com/DataDog/dd-sdk-maui</RepositoryUrl>
    <PackageTags>.NET MAUI iOS Datadog Mobile Binding Internal</PackageTags>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>

    <XCFrameworkPath>../artifacts/DatadogInternal.xcframework</XCFrameworkPath>
    <IsTrimmable>true</IsTrimmable>
  </PropertyGroup>

  <ItemGroup>
    <ObjcBindingApiDefinition Include="ApiDefinition.cs" />
    <ObjcBindingCoreSource Include="StructsAndEnums.cs" />
  </ItemGroup>

  <ItemGroup>
    <NativeReference Include="$(XCFrameworkPath)">
      <Kind>Framework</Kind>
      <SmartLink>True</SmartLink>
      <ForceLoad>True</ForceLoad>
    </NativeReference>
  </ItemGroup>
</Project>
