# Datadog MAUI Sample App - Environment Variables
#
# Copy this file to .env and fill in your Datadog credentials:
#   cp .env.example .env
#   nano .env  # Edit with your credentials
#
# Then use one of these methods:
#
# Method 1: Auto-load .env (recommended)
#   make run-ios        # Automatically loads .env
#   make run-android    # Automatically loads .env
#
# Method 2: Manual source
#   source .env
#   make sample-ios
#   make sample-android
#
# Method 3: Direct export
#   export DD_RUM_IOS_CLIENT_TOKEN="your-token"
#   export DD_RUM_IOS_APPLICATION_ID="your-app-id"
#   make sample-ios

# =============================================================================
# iOS Configuration
# =============================================================================

# iOS Client Token (from Datadog UI: Organization Settings → Client Tokens)
DD_RUM_IOS_CLIENT_TOKEN=

# iOS RUM Application ID (from Datadog UI: RUM → Applications → Your iOS App)
DD_RUM_IOS_APPLICATION_ID=

# =============================================================================
# Android Configuration
# =============================================================================

# Android Client Token (from Datadog UI: Organization Settings → Client Tokens)
DD_RUM_ANDROID_CLIENT_TOKEN=

# Android RUM Application ID (from Datadog UI: RUM → Applications → Your Android App)
DD_RUM_ANDROID_APPLICATION_ID=

# =============================================================================
# General Configuration (Optional)
# =============================================================================

# Environment: dev, staging, production
DD_ENV=dev

# Service name
DD_SERVICE_NAME=shopist-maui-demo

# Datadog site: US1, US3, US5, EU1, AP1, GOV
DD_SITE=US1

# Enable verbose logging: true or false
DD_VERBOSE_LOGGING=true

# Session sample rate: 0-100
DD_SESSION_SAMPLE_RATE=100

# Session replay sample rate: 0-100
DD_SESSION_REPLAY_SAMPLE_RATE=100
